
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow read and write access to the materials folder for any authenticated user.
    match /materials/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
